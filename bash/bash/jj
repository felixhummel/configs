# vim: set ft=bash :
# shellcheck disable=SC1090
if hash jj 2>/dev/null; then
    source <(jj util completion bash)
    alias j=jj
    # complete -p jj | sed -e 's/jj$/j/'
    complete -F _jj -o nosort -o bashdefault -o default j
fi
