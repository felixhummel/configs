$include /etc/inputrc

# to print a keyseq run the following (see man 3 readline)
# cat -v | sed 's;\^\[;\\e;'

# arrow up/down for prefix-based history
"\e[A":history-search-backward
"\e[B":history-search-forward

# Alt+Up/Down to traverse dir tree
"\e[1;3A": "pushd ..\n"
"\e[1;3B": "popd\n"
